#include "Interface.hpp"

SweeperGrid::SweeperGrid(sf::Transformable Container) {
    
    for (int row = 0; row < 8; row++) { // For now we only do 9x9
        for (int col = 0; col < 8; col++) {
        }  
    }

}